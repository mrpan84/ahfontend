import {
  computed,
  createElementBlock,
  normalizeClass,
  openBlock,
  toDisplayString,
  unref
} from "./chunk-G4DFXOZZ.js";

// node_modules/@voidsolutions/vue-dateformat/dist/vue-dateformat.es.js
var m = {
  __name: "DateFormat",
  props: {
    date: {
      type: [String, Date],
      default: /* @__PURE__ */ new Date()
    },
    hasTime: {
      type: Boolean,
      default: false
    },
    noSeconds: {
      type: Boolean,
      default: false
    },
    noDays: {
      type: Boolean,
      default: false
    },
    noYears: {
      type: Boolean,
      default: false
    },
    noMonths: {
      type: Boolean,
      default: false
    },
    shortMonth: {
      type: Boolean,
      default: false
    },
    classes: {
      type: String,
      default: ""
    },
    locales: {
      type: [String, Array],
      default: navigator.languages
    }
  },
  setup(n) {
    const t = n, l = computed(() => {
      let e = {};
      return e.year = "numeric", t.noDays || (e.day = "numeric"), t.noYears || (e.year = "numeric"), t.noMonth || (e.month = "numeric"), t.shortMonth ? e.month = "short" : e.month = "long", t.hasTime && (e.hour = "numeric", e.minute = "numeric", t.noSeconds || (e.second = "numeric")), e;
    }), s = computed(() => {
      let e = "";
      return t.small && (e += "dateformat-small"), e;
    });
    return (e, f) => (openBlock(), createElementBlock("span", {
      class: normalizeClass(unref(s))
    }, toDisplayString(new Date(t.date).toLocaleString(n.locales, unref(l))), 3));
  }
};
var d = {
  install: (n, t) => {
    n.component("DateFormat", m);
  }
};
export {
  d as default
};
//# sourceMappingURL=@voidsolutions_vue-dateformat.js.map
